CREATE TABLE ACCOUNT_TBL (
        ACCOUNT_NUMBER VARCHAR2(10) PRIMARY KEY,
        CUSTOMER_NAME  VARCHAR2(15),
        BALANCE        NUMBER(10,2)
    );

INSERT INTO ACCOUNT_TBL VALUES ('1234567890','Reddy',8000);
SQL> INSERT INTO ACCOUNT_TBL VALUES ('1234567891','Mahesh',0);
SQL> INSERT INTO ACCOUNT_TBL VALUES ('1234567892','Dhanu',100);
SQL> INSERT INTO ACCOUNT_TBL VALUES ('1234567893','Sam',500);

CREATE TABLE TRANSFER_TBL (
       TRANSACTION_ID NUMBER(4) PRIMARY KEY,
        ACCOUNT_NUMBER VARCHAR2(10),
        BENEFICIARY_ACCOUNT_NUMBER VARCHAR2(10),
        TRANSACTION_DATE DATE,
        TRANSACTION_AMOUNT NUMBER(10,2)
  );
  
  
 BANKDAO QUERIES:

"SELECT transactionId_seq1.NEXTVAL from dual"
"SELECT * FROM ACCOUNT_TBL WHERE ACCOUNT_NUMBER = ?" 
"SELECT Balance From ACCOUNT_TBL WHERE ACCOUNT_NUMBER = ?"
"UPDATE ACCOUNT_TBL SET Balance = ? WHERE ACCOUNT_NUMBER = ?"
"INSERT INTO Transfer_TBL VALUES(?,?,?,?,?)"


SEQUENCE QUERIES:

create sequence transactionId_seq1
start with 1000
minvalue 1000
maxvalue 9999
increment by 1;

select transactionId_seq1.NEXTVAL from dual;
